# Зависимости рабочей версии. Ставятся в .venv одной командой:
#   python3.12 -m venv .venv && ./.venv/bin/pip install -r requirements.txt
# Тяжёлого здесь ничего нет: вся обработка кадра — numpy.

numpy==2.5.3          # вся математика: плоскость, карта высот, раскраска
pillow==12.3.0        # картинка кадра в PNG/JPEG для браузера и снимков
fastapi==0.141.1      # веб-сервер: пульт и страница проекции
uvicorn==0.53.0       # запуск веб-сервера

pyyaml==6.0.3         # настройки config/*.yaml
zstandard==0.25.0     # сжатие записей занятия (.hmz)

pytest==9.1.1         # автотесты
