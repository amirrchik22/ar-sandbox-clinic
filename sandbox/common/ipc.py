"""Обмен между процессами.

Кадры — кольцевой буфер в разделяемой памяти (multiprocessing.shared_memory),
метаданные и события — ZeroMQ PUB/SUB. Реализация буфера — этап 1.
"""
FRAMES_SHM = "sandbox_frames"           # имя сегмента разделяемой памяти
EVENTS_ENDPOINT = "tcp://127.0.0.1:5555"  # PUB: события и пульс процессов
CONTROL_ENDPOINT = "tcp://127.0.0.1:5556"  # REP: команды консоли (старт/стоп/режим)
